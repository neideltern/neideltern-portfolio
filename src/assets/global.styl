// FUNCTIONS

hsb($h-hsb, $s-hsb, $b-hsb, $a-hsb = 1)
    if $b-hsb == 0
        return hsla(0, 0, 0, $a-hsb)
    else
        $l-hsl = ($b-hsb / 2) * (2 - ($s-hsb / 100))
        $s-hsl = ($b-hsb * $s-hsb) / ($l-hsl < 50 ? $l-hsl * 2 : 200 - $l-hsl * 2)
        return hsla($h-hsb, $s-hsl, $l-hsl, $a-hsb)

opacify($color, $alpha)
   return transparentify($color, $color, $alpha) 

// MIXINS

text-overflow($lines = 1)
    if $lines == 1
        overflow: hidden
        white-space: nowrap
        text-overflow: ellipsis
    else 
        overflow: hidden
        display: -webkit-box
        -webkit-box-orient: vertical
        -webkit-line-clamp: $lines

// screen breakpoints

desktop-lg()
    @media (min-width:  1920px)
        { block }

desktop-md()
    @media (min-width:  1280px)
        { block }

desktop-sm()
    @media (max-width: 1280px)
        { block }

tablet-lg()
    @media (max-width: 1024px)
        { block }

tablet()
    @media (max-width: 768px)
        { block }

mobile()
    @media (max-width: 430px)
        { block }

// FONTS
// IBM Plex Sans

@font-face
    font-family: 'IBM Plex Sans'
    font-style: normal
    font-weight: 300
    src: url(@/assets/fonts/ibm-plex-sans-light.ttf) format('truetype')

@font-face
    font-family: 'IBM Plex Sans'
    font-style: normal
    font-weight: 400
    src: url(@/assets/fonts/ibm-plex-sans-regular.ttf) format('truetype')

@font-face
    font-family: 'IBM Plex Sans'
    font-style: normal
    font-weight: 500
    src: url(@/assets/fonts/ibm-plex-sans-medium.ttf) format('truetype')

// IBM Plex Mono

@font-face
    font-family: 'IBM Plex Mono'
    font-style: normal
    font-weight: 300
    src: url(@/assets/fonts/ibm-plex-mono-light.ttf) format('truetype')

@font-face
    font-family: 'IBM Plex Mono'
    font-style: normal
    font-weight: 400
    src: url(@/assets/fonts/ibm-plex-mono-regular.ttf) format('truetype')

@font-face
    font-family: 'IBM Plex Mono'
    font-style: normal
    font-weight: 500
    src: url(@/assets/fonts/ibm-plex-mono-medium.ttf) format('truetype')

// Ndtr Icons

@font-face
    font-family: 'Ndtr Icons'
    src: url(@/assets/fonts/ndtr-icons.woff) format('woff')

.icon,
.pseudo-icon:before,
.pseudo-icon:after
    font-family: 'Ndtr Icons'
    font-weight: normal
    font-style: normal
    word-wrap: normal
    letter-spacing: normal
    text-transform: none
    white-space: nowrap
    font-size: inherit
    color: inherit
    line-height: 1

// GLOBAL VARIABLES
// colors
$body-background = hsb(220, 24, 2)
$element-background = hsb(220, 18, 8)
$element-foreground = hsb(220, 16, 12)
$text-color-primary = hsb(220, 6, 100)
$text-color-secondary = hsb(220, 8, 86)
$text-color-tertiary = hsb(220, 10, 72)
// gap
$gap-xl = 32px
$gap-lg = 24px
$gap-md = 12px
$gap-sm = 6px
// height
$control-height-lg = 42px
$control-height-md = 32px
// border radius
$card-border-radius = 12px
// font size
$font-size-xl = 24px
$font-size-hg = 20px
$font-size-lg = 18px
$font-size-md = 16px
$font-size-sm = 14px
$font-size-xs = 12px
// font family
$sans = 'IBM Plex Sans'
$mono = 'IBM Plex Mono'
$icon = 'Ndtr Icons'
// font weight
$light = 300
$regular = 400
$medium = 500
// timing functions
$ease-out-back = cubic-bezier(0.32, 1.56, 0.64, 1)

// ADAPTIVE VATIABLES
:root
    // margin
    --page-margin: 12px
    --header-margin-top: 64px
    --header-margin-bottom: 48px
    // padding
    --page-padding: 12px
    --container-padding-vertical: 48px
    --container-padding-horizontal: 64px
    // font size
    --title-font-size: $font-size-xl
    // border radius
    --hero-border-radius: 32px

    +desktop-sm()
        // padding
        --container-padding-vertical: 32px
        --container-padding-horizontal: 48px

    +tablet-lg()
        // margin
        --page-margin: 6px
        --header-margin-top: 48px
        --header-margin-bottom: 32px
        // padding
        --page-padding: 6px
        --container-padding-vertical: 32px
        --container-padding-horizontal: 24px
        // font size
        --title-font-size: $font-size-hg

    +mobile()
        // margin
        --page-margin: 6px
        --header-margin-top: 48px
        --header-margin-bottom: 32px
        // padding
        --page-padding: 0px
        --container-padding-vertical: 24px
        --container-padding-horizontal: 16px
        // border radius
        --hero-border-radius: 24px
        // font size
        --title-font-size: $font-size-lg